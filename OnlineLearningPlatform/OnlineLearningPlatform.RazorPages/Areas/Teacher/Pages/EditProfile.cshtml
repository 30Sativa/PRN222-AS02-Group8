@page
@model OnlineLearningPlatform.RazorPages.Areas.Teacher.Pages.EditProfileModel
@{
    ViewData["Title"] = "Chỉnh sửa hồ sơ";
}

<div class="page-header fade-in">
    <h1>✏️ Chỉnh sửa hồ sơ giảng viên</h1>
    <span class="breadcrumb-text">
        <a asp-area="Teacher" asp-page="/Dashboard" style="color:var(--primary);">Dashboard</a> →
        <a asp-area="Teacher" asp-page="/Profile" style="color:var(--primary);">Hồ sơ</a> →
        Chỉnh sửa
    </span>
</div>

<!-- Thông báo -->
@if (Model.SuccessMessage != null)
{
    <div class="olp-alert olp-alert-success fade-in">✅ @Model.SuccessMessage</div>
}
@if (Model.ErrorMessage != null)
{
    <div class="olp-alert olp-alert-danger fade-in">❌ @Model.ErrorMessage</div>
}

<div class="olp-card fade-in">
    <div class="olp-card-header">
        <h2>📝 Cập nhật thông tin</h2>
    </div>
    <div class="olp-card-body">
        <form method="post">
            <!-- Email (chỉ đọc) -->
            <div class="olp-form-group">
                <label>📧 Email</label>
                <input type="text" class="olp-form-control" value="@Model.CurrentEmail" disabled />
                <span class="olp-form-hint">Email không thể thay đổi.</span>
            </div>

            <!-- Họ tên -->
            <div class="olp-form-group">
                <label asp-for="Input.FullName">👤 Họ và tên</label>
                <input asp-for="Input.FullName" class="olp-form-control" placeholder="Nhập họ tên" />
            </div>

            <!-- Bio (quan trọng cho Teacher - học viên sẽ xem) -->
            <div class="olp-form-group">
                <label asp-for="Input.Bio">📝 Giới thiệu bản thân</label>
                <textarea asp-for="Input.Bio" class="olp-form-control" rows="4" placeholder="Mô tả kinh nghiệm, chuyên môn giảng dạy của bạn. Học viên sẽ đọc phần này khi chọn khóa học..."></textarea>
                <span class="olp-form-hint">💡 Một bio hay sẽ giúp thu hút thêm học viên!</span>
            </div>

            <!-- SĐT -->
            <div class="olp-form-group">
                <label asp-for="Input.PhoneNumber">📱 Số điện thoại</label>
                <input asp-for="Input.PhoneNumber" class="olp-form-control" placeholder="Nhập số điện thoại" />
            </div>

            <!-- Nút bấm -->
            <div class="form-actions">
                <button type="submit" class="btn-success-olp">💾 Lưu thay đổi</button>
                <a asp-area="Teacher" asp-page="/Profile" class="btn-secondary-olp">← Hủy</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
