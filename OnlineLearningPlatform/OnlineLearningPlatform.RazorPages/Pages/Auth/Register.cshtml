@page
@model OnlineLearningPlatform.RazorPages.Pages.Auth.RegisterModel
@{
    ViewData["Title"] = "Đăng ký";
    Layout = "_Layout";
}

<div class="auth-screen">
    <div class="auth-card fade-up">

        <div class="auth-logo">
            <div class="logo-mark">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M2 3h6a4 4 0 0 1 4 4v14a3 3 0 0 0-3-3H2z"/>
                    <path d="M22 3h-6a4 4 0 0 0-4 4v14a3 3 0 0 1 3-3h7z"/>
                </svg>
            </div>
            <h1>Tạo tài khoản</h1>
            <p>Tham gia EduLearn hôm nay</p>
        </div>

        @if (!ViewData.ModelState.IsValid)
        {
            <div class="alert alert-danger mb-5" asp-validation-summary="All"></div>
        }

        <form method="post">
            <div class="form-group">
                <label asp-for="RegisterRequest.Email">Email</label>
                <input asp-for="RegisterRequest.Email" class="form-control" type="email"
                       placeholder="you@example.com" autocomplete="email" />
                <span asp-validation-for="RegisterRequest.Email" class="form-error"></span>
            </div>

            <div class="form-group">
                <label asp-for="RegisterRequest.FullName">Họ và tên</label>
                <input asp-for="RegisterRequest.FullName" class="form-control"
                       placeholder="Nguyễn Văn A" autocomplete="name" />
                <span asp-validation-for="RegisterRequest.FullName" class="form-error"></span>
            </div>

            <div class="form-group">
                <label asp-for="RegisterRequest.Password">Mật khẩu</label>
                <input asp-for="RegisterRequest.Password" class="form-control" type="password"
                       placeholder="Tối thiểu 6 ký tự" autocomplete="new-password" />
                <span asp-validation-for="RegisterRequest.Password" class="form-error"></span>
            </div>

            <button type="submit" class="btn btn-primary" style="width:100%; padding: 12px; font-size: 15px; margin-top: 8px;">
                Tạo tài khoản
            </button>
        </form>

        <div class="auth-footer">
            Đã có tài khoản?
            <a asp-page="/Auth/Login">Đăng nhập</a>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}